export default interface IConfig {
  apiPrefix: string;
  corsOrigin: string;
  mongoUrl: string;
  nodeEnv: string;
  port: string;
  serviceName: string;
  secret: string;
  swaggerUrl: string;
  swaggerDefinition: string;
}
